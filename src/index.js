import ReactDOM from "react-dom/client"; //importing react dom obj from react dom library and react (seen in pkgjson)

import "./index.css";  
import App from "./App";

const root = ReactDOM.createRoot(document.getElementById("root")); 
 /
root.render(<App />);
